export enum PartnerStatus {
  ACTIVE = 'ACTIVE',
  SUSPENDED = 'SUSPENDED',
}

export enum CommissionStatus {
  EARNED = 'EARNED',
  REVERSED = 'REVERSED',
  PAID = 'PAID',
}

export enum PayoutCycleStatus {
  OPEN = 'OPEN',
  READY = 'READY',
  PAID = 'PAID',
}
