export enum OrganizationStatus {
  ACTIVE = 'ACTIVE',
  SUSPENDED = 'SUSPENDED',
}

export enum OrganizationMemberRole {
  ADMIN = 'ADMIN',
  MEMBER = 'MEMBER',
}

export enum SeatPackSource {
  MANUAL = 'MANUAL',
  PURCHASE = 'PURCHASE',
}

export enum InvitationStatus {
  PENDING = 'PENDING',
  ACCEPTED = 'ACCEPTED',
  REVOKED = 'REVOKED',
}
